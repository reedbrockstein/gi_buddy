import 'package:flutter/material.dart';
class MeasureWidget extends StatelessWidget {
  final Color color;

  MeasureWidget(this.color);

  @override
  Widget build(BuildContext context) {

    return Container(
      color: color,
      child: Text('cfghjkjhgfghjk'),
    );
  }
}